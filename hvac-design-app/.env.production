# Production Environment Configuration
# Used when building for production deployment

# App Configuration
NEXT_PUBLIC_APP_NAME=SizeWise HVAC Canvas
NEXT_PUBLIC_APP_VERSION=0.1.0

# Feature Flags - Production Settings
NEXT_PUBLIC_ENABLE_WEATHER_API=false
NEXT_PUBLIC_ENABLE_ANALYTICS=false
NEXT_PUBLIC_ENABLE_CLOUD_SYNC=false

# Production Settings
NEXT_PUBLIC_DEBUG_MODE=false
NODE_ENV=production

# Security
# IMPORTANT: Never commit secrets to version control
# Use environment variables or secret management services for:
# - API keys
# - Database credentials
# - Authentication secrets
