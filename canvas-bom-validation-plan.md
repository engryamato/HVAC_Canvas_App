# BOM and Validation Context Plan

## Goal
Document the purpose, data points, and states of **all** UI elements in the `Canvas_BOM.pen` and `Canvas_Validation.pen` files into their respective `README.md` files, and update their `context` properties.

## Tasks
- [x] Task 1: Document BOM Panel UI (Header, Toolbar, Chips, Content List, Footer) -> Verify: `docs/Pencil/Canvas/BOM/README.md` includes these sections.
- [x] Task 2: Document BOM Canvas Overlay Elements (BOM Count Badge, BOM Panel Hint) -> Verify: Covered in BOM README.
- [x] Task 3: Update `context` property for BOM elements in `Canvas_BOM.pen` -> Verify: `mcp_pencil_batch_design` completes without error.
- [x] Task 4: Document Validation Panel UI (Summary Section, Issues Section, Actions Section) -> Verify: `docs/Pencil/Canvas/Validation/README.md` created.
- [x] Task 5: Update `context` property for Validation elements in `Canvas_Validation.pen` -> Verify: `mcp_pencil_batch_design` completes without error.

## Done When
- [x] Both `README.md` files cover every specific UI piece from the `.pen` files.
- [x] The Pencil `.pen` files have the `context` attribute correctly assigned to their respective frames.
